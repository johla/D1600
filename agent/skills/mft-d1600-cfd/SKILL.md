---
name: mft-d1600-cfd-execution
description: Drive the generic D1600 passive stormwater separator to a machine-gated computational design-search release.
---

Run `make check`, `make surrogate`, `make status` and `make demo-gate` first.

Read the root execution contract and work as verified state transitions. The surrogate is not CFD evidence. Continue across geometry, meshing, hydraulics, particles, sensitivity, optimization, reporting and red-team criteria until the project gate passes. Intermediate task completion is never project completion.
